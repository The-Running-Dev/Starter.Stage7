namespace Starter.Bootstrapper
{
    /// <summary>
    /// Represents the type of setup to
    /// be used when registering dependencies
    /// </summary>
    public enum SetupType
    {
        Debug,
        Release,
        Test
    }
}